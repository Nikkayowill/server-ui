# Production Environment Variables Template
# ==========================================
# INSTRUCTIONS:
# 1. SSH into your production server
# 2. Set these as system environment variables (NOT as a .env file!)
# 3. NEVER commit actual production values to git
# 4. ROTATE ALL KEYS before deploying (your current keys are in dev .env)

# Application
NODE_ENV=production
PORT=3000

# MUST GENERATE NEW - Your current dev keys should be rotated
# Stripe: Dashboard → Developers → API Keys → Click "Roll" to regenerate
SESSION_SECRET=1cf84fb120158df6531c0a5ff49ef2f77ec22e9ae0149dc77a9e8e9570b2a09e51fdd7190df3089e43e0018e75366dcbfea401d0690caa33e18016f2577d426b
STRIPE_SECRET_KEY=sk_live_REPLACE_WITH_NEW_ROTATED_KEY
STRIPE_WEBHOOK_SECRET=whsec_REPLACE_WITH_NEW_WEBHOOK_SECRET

# DigitalOcean: API → Tokens → Delete old, Generate new
DIGITALOCEAN_TOKEN=dop_v1_REPLACE_WITH_NEW_ROTATED_TOKEN

# Database - Use strong password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=webserver_db
DB_USER=basement_app
DB_PASSWORD=KExFqHy/QmvpiSW1d9Z9gkMcZaK4GFUvvJdOUbKZvO0=

# How to set on production server (Ubuntu/Debian):
# Option 1: System-wide (recommended)
# sudo nano /etc/environment
# Add each line: VARIABLE_NAME="value"
# Then: source /etc/environment

# Option 2: PM2 with environment variables
# pm2 start ecosystem.config.js --env production
# pm2 set <variable_name> <value>

# Option 3: Export in shell (temporary)
# export NODE_ENV=production
# export SESSION_SECRET="your_secret_here"
# etc...
